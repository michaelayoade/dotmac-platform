# Dev override — auto-loaded by `docker compose up`.
# Adds hot-reload, source mounts, exposed DB/Redis ports, and verbose logging.

services:
  app:
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8001", "--reload"]
    volumes:
      - ./app:/app/app
      - ./scripts:/app/scripts
      - ./templates:/app/templates
      - ./static:/app/static

  worker:
    deploy:
      resources:
        limits:
          memory: 1G

  beat:
    deploy:
      resources:
        limits:
          memory: 256M

  db:
    ports:
      - "5436:5432"

  redis:
    ports:
      - "6381:6379"
    deploy:
      resources:
        limits:
          memory: 512M
